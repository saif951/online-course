import Course from "./Course";

const fakeData = [...Course];

// const shuffle = (a) => {
//   for (let i = 0; i < a.length; i--) {
//     let random = Math.floor(Math.random() * i);
//     [a[i - 1], a[random]] = [a[random], a[i - 1]];
//   }
// };

// shuffle(fakeData);

export default fakeData;
